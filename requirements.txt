# Core training stack (tested on Python 3.10/3.11, CUDA 12.x)
torch>=2.3.0
transformers>=4.44.0
peft>=0.12.0
trl>=0.9.6
datasets>=2.20.0
accelerate>=0.33.0
bitsandbytes>=0.43.0      # 4-bit QLoRA (GPU only)
sentencepiece>=0.2.0
# Eval
jsonschema>=4.22.0
